server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowsync_simple?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# SpringDoc OpenAPI
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# 千问 API Key（通过环境变量注入，默认值仅用于开发调试）
dashscope:
  api-key: ${DASHSCOPE_API_KEY:sk-your-api-key-here}

# 日志级别
logging:
  level:
    hgc.flowsyncapi: debug
